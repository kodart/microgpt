[package]
name = "microgpt"
version = "0.1.0"
edition = "2024"
description = "The most atomic way to train and run inference for a GPT, in dependency-free Rust"

[dependencies]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
debug = false
